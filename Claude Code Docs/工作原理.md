
### 上下文窗口

要控制在压缩期间保留的内容，请在CLAUDE.md中添加“Compact Instructions”部分或使用焦点运行`/compact`（如`/compact focus on the API changes`）

运行`/context`以查看什么在占用空间


### 有效使用

- 不需要完美的提示，从想要的开始
`修复登录错误`

[claud 调查，尝试一些东西]
`这不太对，问题在于会话处理。`

#### 预先具体

```
结账流程对于持有过期卡的用户来说已损坏
检查 src/payments/ 中的问题，特色是令牌刷新。
首先编写一个失败的测试，然后修复它
```

#### 给Claude一些东西来验证

```
实现 validateEmail。 测试用例：'user@example.com' → true，
'invalid' → false， 'user@.com' → false， 之后运行测试。
```


#### 在实现前探索

对于复杂的问题，将研究与编码分开。使用plan mode

```
读取 src/auth 并理解我们如何处理会话
然后添加 OAuth 支持创建一个计划
```


#### 委派、不要指示

```
结账流程对于持有过期卡的用户来说已损坏。
相关代码在 src/payments/ 中。你可以调查并修复它吗？
```